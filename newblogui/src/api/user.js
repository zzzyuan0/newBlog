import {get,post} from "./http"

export const getCommentApi = (p) => get("/user/comment/get", p)

export const addCommenApi = (p) => get("/user/comment/get", p)

export const addCommentHeatApi = (p) => get("/user/comment/addHeat", p)


